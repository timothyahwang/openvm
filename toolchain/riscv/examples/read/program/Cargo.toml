[workspace]
[package]
version = "0.1.0"
name = "axvm-read-program"
edition = "2021"

[dependencies]
axvm = { path = "../../../axvm" }
serde = { version = "1.0", default-features = false, features = [
    "alloc",
    "derive",
] }
