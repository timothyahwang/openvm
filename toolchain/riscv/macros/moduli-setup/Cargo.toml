[package]
name = "axvm-moduli-setup"
version = { workspace = true }
edition = "2021"

[dependencies]
syn = { version = "2.0", features = ["full"] }
quote = "1.0"
axvm-macros-common = { path = "..", default-features = false }

[lib]
proc-macro = true