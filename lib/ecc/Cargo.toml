[package]
name = "axvm-ecc"
version.workspace = true
authors.workspace = true
edition.workspace = true
description = "axVM elliptic curve cryptography library."

[dependencies]
axvm = { workspace = true }

[features]
default = []
std = ["axvm/std"]