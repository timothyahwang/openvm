[workspace]
[package]
name = "axvm-fibonacci-program"
version = "0.0.0"
edition = "2021"

[dependencies]
axvm = { path = "../../../crates/toolchain/axvm" }

[features]
default = []
std = ["axvm/std"]
