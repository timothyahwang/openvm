[workspace]
[package]
version = "0.1.0"
name = "axvm-regex-program"
edition = "2021"

[dependencies]
axvm = { path = "../../../toolchain/riscv/axvm" }
axvm-keccak256-guest = { path = "../../../extensions/keccak256/guest" }
regex = { version = "1.11.1", default-features = false }

[features]
default = []
std = ["axvm/std"]
